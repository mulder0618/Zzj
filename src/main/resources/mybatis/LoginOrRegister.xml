<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zzj.login.mapper.LoginOrRegisterMapper">
    <!-- 插入绑卡详情表 -->
 <!--   <insert id="insertBindcardDetail" parameterType="com.zhx.gateway.entity.BindcardDetail">
        INSERT INTO gateway_order_bindcard_detail
        (
          gatewayOrderNo,returnUrl,notifyUrl,updateDate,createDate,merchantSendDate,status,
          backOrderNo,uid,dfp
        )
        VALUES
        (
          #{gatewayOrderNo},#{returnUrl},#{notifyUrl},#{updateDate},#{createDate},#{merchantSendDate},
          #{status},#{backOrderNo},#{uid},#{dfp}
        )
    </insert>

    &lt;!&ndash; 更新绑卡状态&ndash;&gt;
    <update id="updateBindcardStatus" parameterType="com.zhx.gateway.entity.BindcardDetail">
        UPDATE gateway_order_bindcard_detail
        <set>
        <if test="status != null">
            status = #{status},
        </if>
        <if test="updateDate != null">
            updateDate = #{updateDate},
        </if>
        <if test="backOrderNo != null">
            backOrderNo = #{backOrderNo},
        </if>
        </set>
        WHERE gatewayOrderNo=#{gatewayOrderNo}
    </update>-->


    <!-- 查询用户信息-->
    <select id="selectUserinfo" parameterType="java.util.HashMap" resultType="java.util.HashMap">
        SELECT  *
        FROM   userinfo t
        WHERE t.loginName=#{loginName}
        AND   t.status=#{status}
    </select>

</mapper>